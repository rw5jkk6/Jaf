- ログを削除することでOSが軽くなり、起動が安定する
- どれだけログがあるかチェック
  - `sudo journalctl --disk-usage`
- ログを消去する。(100MBまで削除する)
  - `sudo journalctl --vacuum-size=100M`   
- ログの上限を設定する
  - `sudo vim /etc/systemd/journald.conf`
  - 次のを一番下に書き込む
   ```
   SystemMaxUse=100M
   SystemKeepFree=300M
   ```
  - `sudo systemctl restart systemd-journald`
