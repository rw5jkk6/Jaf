## CMS(wordpressを含む)を見たら共通してすること
- GitHubにコードが公開されていることがあるので見る
- そのcms特有の脆弱性
- ダッシュボードにログインできれば、reverse-shellが書き込めないかチェックする
- 設定ファイルでユーザとパスワードをチェックしてデータベースを見る


## WordPressチートシート

- (前提)wordpressの侵入までのチートシートであるが、他の方法(他のポートなど)で侵入することができることもある
- ドメインネームを設定する
  - `sudo vim /etc/hosts` 
- 脆弱性を探す
  - wpscanでpluginやテーマに脆弱性がないかを調べる。脆弱性があれば、ここで侵入することができる
  - `wpscan -u http://ドメインネーム -e ap`またはmsfconsoleの`auziliary/scanner/http/wordpress_scanner`
  - wpscanのAPIトークンを使うと脆弱性を見つけることができる。これが一番最強
- ダッシュボードのユーザを探す
  - サイトのどこかに書いてある。なければwpscanで探す
  - `wpscan -u http://ドメインネーム -e u`
  - ユーザがわかれば、ここでhydraのrockyou.txtを使って、sshすることもできる
- ダッシュボードのユーザのパスワードを探す
  - passwordまたはpassword辞書を探す
    - サイトのどこかに書いてある可能性がある。
    - cewlで辞書を作ることもある
    - ディレクトリリスティングで辞書をダウンロードできるかもしれない
    - なければrockyou.txtを使う
  - `wpscan -u http://ドメインネーム -U ユーザ名 -P パスワードファイル` 
- sshしてみる
  - ダッシュボードのパスワードとsshのパスワードを使いまわしているかもしれない 
- ダッシュボードにログインする
  - adminでログインしたらmsfconsoleのwp_adminを使うか、pluginをuploadしてreverse-shellをする 
- 侵入したら
  - config.phpを見て、SQLのユーザをパスワードを見る。このパスワードがsshでそのまま使えることがある。使えなければ、SQLに接続してユーザ名とパスワードを探す
  - wordpressはどこのディレクトリにあるか
    - 基本的には`/var/www/`だが、例えばMrRobotだと`/opt/bitnami/apps/`にある
    - どこにあるかは`find / -name wp-config.php`この方法で探す

- 正規のユーザになる
  - 以降は、いつもと同じシステム内探索ランキングなどを利用する 

## CMSチートシート(wordpress以外)

- (前提)CMSの侵入までのチートシートであるが、他の方法(他のポートなど)で侵入することができることもある
- 脆弱性を探す
  - CMSの名前とversionをexploitdbで検索して脆弱性を探す
- ダッシュボードのユーザを探す
  - サイトのどこかに書いてある。
  - ユーザがわかれば、ここでhydraのrockyou.txtを使って、sshすることもできる
- ダッシュボードのユーザのパスワードを探す
  - passwordまたはpassword辞書を探す
    - サイトのどこかに書いてある可能性がある。
    - cewlで辞書を作ることもある
    - ディレクトリリスティングで辞書をダウンロードできるかもしれない
    - なければrockyou.txtを使う
- sshしてみる
  - ダッシュボードのパスワードとsshのパスワードを使いまわしているかもしれない 
- ダッシュボードにログインする
  - adminでログインしたらファイルに書き込む、またはpluginをuploadしてreverse-shellをする 
- 侵入したら
  - config.phpを見て、SQLのユーザをパスワードを見る。このパスワードがsshでそのまま使えることがある。使えなければ、SQLに接続してユーザ名とパスワードを探す 
- 正規のユーザになる
  - 以降は、いつもと同じシステム内探索ランキングなどを利用する 
